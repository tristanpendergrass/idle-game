When I added Hexes it looked like:

* Change sidebar to make new skill not "Under Construction" in `Drawer.elm`
* Set up `Hexes.elm` and reference it from `Content.elm`
* Use `Utils.skills` classes to build out skeleton
* 